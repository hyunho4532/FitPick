import { Text } from "react-native";
import { ContainerInFlex } from "../styles/child";

export default function ProfileScreen() {
  return (
    <ContainerInFlex>
      <Text>진행하려면 현재 로그인을 진행해주세요!</Text>
    </ContainerInFlex>
  );
}