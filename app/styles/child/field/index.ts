import { StyleSheet } from 'react-native'

export const TextInputStyle = StyleSheet.create({
    textInput: {
        width: 360,
        height: 40,
        borderWidth: 1,
        borderColor: "gray"
    }
})