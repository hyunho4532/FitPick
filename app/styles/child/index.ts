import styled from '@emotion/native'
import { Container, Text } from '../base'

export const ContainerInFlex = styled(Container)`
    flex: 1;
`

export const TitleText = styled(Text)`
    font-size: 24px;
    font-weight: bold;
`